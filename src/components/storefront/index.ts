/*
 * PARKED. Neither component is rendered today (owner, 2026-09-15): the ticker
 * was hidden and the flagship shop does not belong on the homepage. Both keep
 * their builders in `server/storefront` and their copy under `home.ticker` /
 * `home.shop`, ready to be placed.
 */
export { CatalogTicker, type CatalogTickerCopy, type CatalogTickerItem } from "./CatalogTicker";
export { FlagshipShop, type FlagshipShopCopy, type FlagshipShopProduct } from "./FlagshipShop";
